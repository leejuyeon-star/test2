#include <stdio.h>

int main (void){
    int N,i;
    int coin[N][3]
    scanf("%d", &N);
    getchar();

    for (i=0;i<N;i++){
        scanf("%d %d %d", &coin[i][0], &coin[i][1], &coin[i][2]);
        getchar();
    }

    for(i=0;i<N;i++){
        rank(coin[])
    }



}