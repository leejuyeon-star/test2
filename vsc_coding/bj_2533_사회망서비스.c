#include <stdio.h>

int main(void)
{
    int N;
    scanf("%d", &N);
    int u[N-1], v[N-1];
    for (int i=0;i<N-1;i++)
    {
        scanf("%d %d", &u[i], &v[i]);
    }

    for (i=0;i<N-1;i++)
    {
        //제일 작은 u찾기
    }
    //sum+=1
    //제일 작은 u 모두 제거, u값과 같은 v도 모두 제거
    //제일 작은 u 찾기...반복
}